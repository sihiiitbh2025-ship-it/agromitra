# Which provider to use: openai | ollama | echo
PROVIDER=ollama

# Model names for your chosen provider
# OpenAI example: gpt-4o-mini
# Ollama example: llama3.1:8b
MODEL=llama3.1:8b

# If using OpenAI-compatible API
OPENAI_API_KEY=

# If using Ollama (default host below)
OLLAMA_HOST=http://localhost:11434

# System prompt that sets your bot's behavior
SYSTEM_PROMPT=You are AgriBot, an agriculture assistant. Give direct, concise answers. Do not mention the user's name or reasoning steps. Answer in one paragraph if possible.

